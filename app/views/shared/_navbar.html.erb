<nav class="navbar navbar-expand-lg navbar-light" id="navbar" >
  <a class="navbar-brand" href="/">Pet Place</a>
  <%# <span class="navbar-brand mb-0 h1">Pet Place</span> %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      
       
    </div>
    </div>

    <div class="navbar-nav">
        <%= link_to "Listings", listings_path, class:"nav-item nav-link"%> 
        <%= link_to "Organisations", organisations_profiles_path, class:"nav-item nav-link" %>
        <%= link_to "Resources", organisations_profiles_path, class:"nav-item nav-link" %>
    </div>
    <div class="navbar-nav">
        <a class="nav-item nav-link" href="#"><button type="button" class="btn btn-primary">Donate</button></a>
    </div>
    <div class="navbar-nav">
        <button type="button" class="btn btn-primary">
        <% if current_user %>
        <%= link_to "Your account", current_user.users_profile_path %>
        <% elsif current_organisation %>
        <%= link_to "Your Account", current_organisation.organisations_profile %>
        <% end %>
        </button>
    </div>
    <div class="navbar-nav">
        <% if user_signed_in? || organisation_signed_in? || admin_signed_in? %>
            <button type="button" class="btn btn-primary"><%= link_to "Logout", destroy_user_session_path || destroy_organisation_session_path || destroy_admin_session_path, method:"delete"%></button></a>
        <% else %>
            <button type="button" class="btn btn-primary"><%= link_to "Login", "/" %></button>
        <% end %>
    </div>

</nav>